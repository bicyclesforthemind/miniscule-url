


const initialState = {
    urls: []
};

const urlReducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
};

export default urlReducer;